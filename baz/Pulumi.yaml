name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: baz
  important: true
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
